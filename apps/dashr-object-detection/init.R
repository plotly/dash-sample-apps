# require(tidyverse)
# require(dashR)
# require(dashCoreComponents)
# require(dashHtmlComponents)
# require(dashPlayer)
# require(plotly)
# require(rlist)
# require(stringr)

# require(data.table)
# require(glue)
# require(compiler)
install.packages(c('tidyverse', 'dashR', 'dashCoreComponents', 'dashHtmlComponents', 'plotly', 'rlist', 'stringr', 'data.table', 'glue'))

##DashPlayer- install the local version inside the repo